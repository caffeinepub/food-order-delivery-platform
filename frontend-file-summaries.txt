{
  "frontend/src/index.css": "Global styles with white and orange (#F97316) OKLCH palette, custom Sora/Inter fonts, status badge utilities, hero gradient, card hover and shake animations — all backgrounds are solid white with no transparent fills.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Sora display font, Inter body font, custom border radii, card/orange shadow tokens, orange color scale, and slide/fade/shake animation keyframes for the white-and-orange food delivery theme.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; adds `useDeleteOrder` mutation that calls `actor.deleteOrder(orderId)`, handles the `#err(#notFound)` result variant, and invalidates both `allOrders` and `ordersByCustomer` query keys on success; all other hooks unchanged.",
  "frontend/src/hooks/useCart.ts": "Custom hook managing shopping cart state with sessionStorage persistence, providing add/remove/update/clear operations and computed total/itemCount.",
  "frontend/src/components/Navigation.tsx": "Sticky top navigation bar with FoodRush logo, links to Customer Portal and Courier Dashboard, active state highlighting, and responsive mobile hamburger menu.",
  "frontend/src/components/Layout.tsx": "Shared layout wrapper using TanStack Router's Outlet, composing Navigation at top and a footer with dynamic year and caffeine.ai attribution link.",
  "frontend/src/components/OrderStatusBadge.tsx": "Reusable status badge component using CSS utility classes (badge-pending, badge-accepted, etc.) with distinct opaque filled colors per order status and animated dot indicator.",
  "frontend/src/components/MenuSection.tsx": "Displays a category section of menu items in a responsive grid with solid white cards and orange controls — uses `CartItem` type from `useCart` hook (with `itemName` field).",
  "frontend/src/components/CartPanel.tsx": "Shopping cart slide-in panel with orange-500 header, solid white item cards, and orange quantity controls — uses `CartItem` type imported from `useCart` hook (with `itemName` field).",
  "frontend/src/components/CheckoutForm.tsx": "Checkout form with solid white background, orange-500 header, and orange Place Order button — uses `useGetCallerProfile` hook and `CartItem` type from `useCart` with `itemName` field.",
  "frontend/src/components/OrderConfirmation.tsx": "Order confirmation screen that uses `useOrderById` with 8-second polling to display live backend status in the progress stepper; accepts `orderId` as a prop and never uses local state or sessionStorage for status; handles loading, error, and cancelled states gracefully with a live polling indicator.",
  "frontend/src/components/OrderCard.tsx": "Courier-facing order card with exactly two action buttons for active orders — a prominent orange \"Deliver\" button (optimistic status update) and a \"Delete\" button with inline confirmation banner; completed/cancelled orders show a status label and a \"Delete Order\" button only.",
  "frontend/src/components/OrderFilters.tsx": "Horizontal scrollable filter pill bar showing only four tabs — All Orders, Pending, Delivered, and Cancelled — with per-status count badges and orange-500 active styling.",
  "frontend/src/pages/CustomerPortal.tsx": "Main customer-facing page that correctly uses `items` (not `cartItems`) from `useCart`, passes the right props to `MenuSection` (`onAddToCart`), `CheckoutForm` (`onOrderPlaced`), `CartPanel` (`items`), and `ProfileSetupModal` (`open`/`onComplete`); removes the non-existent `isAuthenticated` prop from `CustomerNavigation`; persists `confirmedOrderId` to sessionStorage and invalidates order queries after placing an order.",
  "frontend/src/pages/CourierApp.tsx": "Courier Dashboard with simplified two-action workflow (Deliver + Delete); uses optimistic local state for the Deliver action, real backend delete mutation, a three-stat summary row (Pending/Delivered/Cancelled), and the simplified four-option filter bar.",
  "frontend/src/App.tsx": "Root app component with TanStack Router setup using default imports for CustomerPortalLayout, CustomerPortal, and CourierApp, registering both the `/` (customer portal with layout) and `/courier` (courier app standalone) routes.",
  "frontend/src/components/CustomerPortalLayout.tsx": "Layout shell using TanStack Router's Outlet with solid white background, orange-accented footer, dynamic year, and caffeine.ai attribution — exported as default.",
  "frontend/src/components/LoginButton.tsx": "Internet Identity login/logout button component that handles authentication flow, clears query cache on logout, and shows loading state during sign-in.",
  "frontend/src/components/CustomerNavigation.tsx": "Customer Portal sticky navigation bar with solid white background, orange-500 active tab indicators, orange cart badge, and orange Sign In button — no transparent backgrounds.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-time user profile setup modal that prompts authenticated users without a profile to enter their name and phone number; prevents dismissal until saved.",
  "frontend/src/components/MyOrdersView.tsx": "My Orders view that uses `useOrdersByCustomer` (polling every 15s) and displays status badges sourced directly from backend data without any local state caching; includes a manual refresh button and live polling indicator.",
  "frontend/src/components/AccountView.tsx": "Account view with orange-500 header, solid white form with orange focus rings, and orange Save Profile button — uses correct hook names `useGetCallerProfile` and `useSaveCallerProfile`.",
  "frontend/index.html": "HTML entry point with updated browser tab title set to \"The Deccan BHOJAN\".",
  "frontend/src/components/MenuManagementSection.tsx": "Menu management component that now uses `useAdminMenu` (instead of `useMenu`) so all items including unavailable ones remain visible after toggling; adds per-item loading state tracking (`togglingItemId`, `deletingItemId`) for accurate inline spinners, and the toggle handler calls only `toggleMenuItemAvailability` — never `deleteMenuItem`.",
  "frontend/src/hooks/useCourierAccess.ts": "Custom hook managing courier PIN access state with localStorage persistence, exposing hasAccess boolean and grantAccess/revokeAccess functions.",
  "frontend/src/components/CourierPinGate.tsx": "PIN entry screen with solid white background and orange accents — `onAccess` prop now correctly typed as `(pin: string) => boolean` to match `useCourierAccess`'s `grantAccess` signature."
}